<p id="notice"><%= notice %></p>

<pre>
<b>Title</b><%= determine_num_of_spaces("Title") %><%= @resource.title %>
<b>Host</b><%= determine_num_of_spaces("Host") %><%= @resource.host %>
<b>URL</b><%= determine_num_of_spaces("URL") %><%= @resource.url %>
<b>Domain</b><%= determine_num_of_spaces("Domain") %><%= @resource.domain %> 
<% @resource.optionals.each do |o| %>
<b><%= o.name %></b><%= determine_num_of_spaces(o.name) %><%= o.value %>
<% end %>
</pre>

<br />

<%= link_to 'Edit', edit_resource_path(@resource) %> |
<%= link_to 'Back', resources_path %>
